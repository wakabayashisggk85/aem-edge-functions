kind: 'CDN'
version: '1'
metadata:
  envTypes: ["rde"]
data:
  originSelectors:
    rules:
      - name: fr-token-replace
        when: { 
          allOf: [
            { reqProperty: tier, equals: publish },
            { reqProperty: path, like: /content/b2c/fr/fr* }
          ]
        }
        action:
          type: selectAemOrigin
          originName: compute-token-replace
          skipCache: false
